# CLAUDE.md — こぜログ ルールブック

> ルールは随時更新される。更新時はカテゴリ単位で修正し、全体整合性を維持すること。

---

## 1. 会話・応答ルール

- 日本語で応答する
- 口調は**ですます調**で統一する
- 作業前に要件が未確定の場合は、確定するまで作業しない

---

## 2. 参考サイト

データ収集・ページ構成の参考として以下5サイトを使用する。

- https://1geki.jp/
- https://chonborista.com/
- https://p-town.dmm.com/machines
- https://p-gabu.jp/
- https://nana-press.com/kaiseki/

---

## 3. データ収集ルール

### 数値引用ルール
- 5サイトすべての数値が一致する場合 → その数値をそのまま引用する
- サイトごとに数値が異なる場合 → 特定サイトに依存せず、**5サイトの平均値**を引用する
- **5%以上の差異**がある場合 → 作業前にユーザーに確認する

---

## 4. 表示・用語ルール

### 単位
- 差枚数 → **「枚」** で統一（メダル機・スマスロ共通）
- 期待収支 → **「pt」** で統一

### 禁止ワード（法的理由）
| 禁止 | 正しい表現 |
|---|---|
| 円 | pt |
| 換金 | 交換 |

---

## 5. 機種分類ルール

機種は以下の**2軸の組み合わせ**で分類する。

### 軸①｜ゲーム性（遊技タイプ）
| 分類 | 内容 |
|---|---|
| ノーマル機 | ボーナスのみ。AT/ARTなし |
| AT機 | アシストタイム搭載 |
| ART機 | アシスト＋リプレイタイム搭載 |
| A+AT / A+ART | ノーマル＋AT/ARTのハイブリッド |
| ST機 | ボーナス当選後にST（高確率ボーナス状態）が発動し、連続ボーナスを目指すゲーム性。AT/ARTとは異なる出玉機構 |
| +BT（複合表記） | ボーナストリガー搭載機。通常時にトリガーを貯めてボーナスを発動する追加メカニズム。打ち方・やめ時がBTなし機と異なるため必ず明記する。表記例：`AT+BT`（AT機にBT搭載）／`ノーマル+BT`（ノーマル機にBT搭載） |

### 軸②｜メディア（ハード方式）
| 分類 | 内容 |
|---|---|
| メダル機 | コイン使用（5号機・6号機） |
| スマスロ機 | コインレス・pt管理 |

### 組み合わせ例
| 機種 | ゲーム性 | メディア |
|---|---|---|
| ジャグラー（6号機） | ノーマル機 | メダル機 |
| スマスロ ハナビ | ノーマル機 | スマスロ機 |
| スマスロ サンダーV | ノーマル機 | スマスロ機 |
| スマスロ カバネリ2 | ST機 | スマスロ機 |

---

## 6. ページ構成ルール

### 必須セクション（全機種・全タイプ共通）
| セクション | ID |
|---|---|
| ボーナス確率・機械割（＋期待収支） | `#spec` |
| 小役確率 | `#koyaku` |
| 設定判別ポイント | `#setting` |
| 設定判別ツール | `#tool` |
| よくある質問（FAQ） | `#faq` |

### 任意セクション（データが揃えば掲載）
| セクション | 条件 |
|---|---|
| リール配列 | 配列データが入手できた場合 |
| 打ち方 | 手順が公開されている場合 |
| ボーナス概要 | ボーナス種類が複数ある場合 |
| 設定変更・朝一恩恵 | 情報が確認できた場合 |
| 公式YouTube動画 | 公式動画が存在する場合（全機種に存在するため実質必須） |

### 公式YouTube動画 配置ルール
- **配置位置：FAQ直後・フッター広告の直前**
- **目次（TOC・サイドバー）には含めない**（目次リンク経由のスキップで広告視認機会が失われるため）
- 動画は `loading="lazy"` を必須とし、`aspect-ratio:16/9` でCLS防止
- `id="sec-movie"` を付与し、著作権表記（「※ {メーカー名} 公式YouTubeチャンネルより」）を必ず添える

#### 動画選定ルール
- **掲載対象：メーカー公式チャンネルの動画のみ**（非公式・個人チャンネルは不可）
- **優先順位：① PR動画 → ② 解析動画**（両方あれば横並び `movie-grid` で掲載）
- **動画埋め込み前に必ずユーザーへ確認を取る**
  1. 公式チャンネルを検索し、PR動画・解析動画のURLを調べる
  2. oEmbed API（`https://www.youtube.com/oembed?url=...&format=json`）で各URLの公開状態を確認する
  3. **「① PR動画: {URL}（公開中 or 非公開）／② 解析動画: {URL}（公開中 or 非公開）」の形式でユーザーに提示し、承認を得てから埋め込む**

### タイプ別セクション

#### ゲーム性で決まるもの
| セクション | ノーマル機 | AT/ART機 | ST機 |
|---|---|---|---|
| 天井・リセット | ✗ | ✅ | ✅ |
| モード詳細 | ✗ | ✅ | ✗ |
| モード別天井 | ✗ | ✅ | ✗ |
| モード別期待値 | ✗ | ✅ | ✗ |
| 天井期待値 | ✗ | ✅ | ✅ |
| AT/ART詳細（純増・継続率） | ✗ | ✅ | ✗ |
| ST詳細（継続率・上位ST種別） | ✗ | ✗ | ✅ |
| フリーズ・高確率ゾーン | ✗ | 任意 | ✅ |
| 有利区間（エンディング含む） | ✗ | ✅ | ✅ |
| ゾーン・上乗せ | ✗ | 任意 | 任意 |

#### メディアで決まるもの
| セクション | メダル機 | スマスロ機 |
|---|---|---|
| 期待収支セクション内のpt換算（交換レート選択） | ✅ | ✅ |

> ※ 新機種追加時に不足セクションが判明した場合は §6 に随時追記する

---

## 7. 機種追加手順

### 自律実行ルール
- 機種追加作業は**ユーザー承認不要で自律的に進めてよい**（データ収集・ページ作成・チェックリスト・SEO施策すべて含む）
- ただし以下は引き続きユーザー確認が必要：
  - **数値に5%以上の差異**がある場合（§3）
  - **公式YouTube動画の埋め込み**（§6 動画選定ルール）

### 基本フロー
```
1. 参考5サイトからスペックデータを収集
2. 数値引用ルール（§3）に従い数値を確定
3. 機種分類（§5）を確認
4. 適用セクション（§6）をチェックリストで確認
5. テンプレートをコピーして機種フォルダに配置
6. データJSファイルを作成・登録
7. 更新チェックリスト（§7.1）を全項目実施
8. SEO施策（§10）を適用：FAQ生成・JSON-LD・OGP・内部リンク・sitemap更新
```

### 7.1 機種追加時の更新チェックリスト

機種ページ作成後、**以下すべてを必ず更新する**。漏れ防止のため作業完了時にチェックすること。

| # | 対象ファイル | 更新内容 | 必須 |
|---|---|---|---|
| 1 | `[maker-slug]/[machine-slug]/index.html` | 機種ページ本体を作成 | ✅ |
| 2 | `[maker-slug]/index.html` | メーカー一覧ページに機種カードを追加（新メーカーの場合はページごと新規作成） | ✅ |
| 3 | `index.html`（TOPページ） | 機種カードを導入日降順の正しい位置に追加し、**常に最新20件のみ**に維持する（21件目以降のHTMLカードは削除する） | ✅ |
| 4 | `assets/machines/index.js` | MACHINES_LIST にエントリを追加（id・name・kana・releaseDate・url・rate等） | ✅ |
| 5 | `sitemap.xml` | 機種ページURL＋新メーカーページURLを追加 | ✅ |
| 6 | `admin/index.html` | ADMIN_OVERRIDES に kozelog:true・kozelogUrl・hasMovie等を反映。**ADMIN_MACHINES（将来台リスト）に該当機種がある場合は `future:true` を外し、kozelog関連プロパティを ADMIN_OVERRIDES 側に移す** | ✅ |

> ※ 1つでも漏れると検索ページ・TOPページ・admin管理画面に不整合が発生する

### 7.2 TOPページ 最新20件ルール

- TOPページ（`index.html`）の機種カード一覧は**常に最新20件のみ**をHTMLに記載する
- カードは**導入日の降順（新しい順）**に並べる。同日の機種間の順序は任意
- 新機種を追加する際は、**導入日で正しい位置にカードを挿入**し、21件目以降になるカードは**HTMLから削除**する
- 見出しの `<small>最新20機種</small>` の件数と実際のカード数を必ず一致させる
- 全機種の一覧は検索ページ（`/search/by-date/`）で参照可能なため、TOPから削除してもユーザーへの影響はない

### 筐体画像ルール

#### 画像仕様
| 項目 | 値 |
|---|---|
| サイズ | **360×640px**（アスペクト比 9:16） |
| 形式 | PNG（透過背景） |
| 保存先 | `[maker-slug]/[machine-slug]/img/cabinet.png` |

#### 画像加工プロンプト（GPT等に渡す用）
機種追加時、筐体画像の加工が必要な場合は以下のプロンプトをユーザーに提示する：

```
以下の筐体画像を加工してください。複数枚ある場合はすべて同じルールで統一してください。

【サイズ・フォーマット】
- 出力サイズ：360×640px（アスペクト比 9:16）
- 出力形式：透過PNG

【筐体の配置】
- 筐体の高さを画像の高さの85%に統一（機種が違っても同じ比率）
- 水平方向：中央揃え
- 垂直方向：やや上寄り（上余白7%・下余白8%目安）

【背景・加工】
- 背景は完全透過（筐体以外をすべて除去）
- 筐体の影・反射・グロー効果は除去し、筐体本体のみ残す

【厳守事項】
- 元画像の筐体デザインを絶対に改変しないでください
- 筐体の色味・コントラスト・ディテールはすべて元画像のまま維持してください
- リサイズ以外の画像補正（シャープネス・彩度・明度調整等）は一切行わないでください
- 筐体を再描画・再生成しないでください。元画像のピクセルをそのまま使用してください
```

#### 筐体画像の提示タイミング
- **新機種追加時**：ページ作成前に「筐体画像はありますか？上記プロンプトで加工できます」とユーザーに確認する
- **ユーザーが「筐体プロンプト」と指示した場合**：上記プロンプトをそのまま出力する

#### 画像保存後の更新
筐体画像を `img/cabinet.png` に保存した場合、以下を必ず同時に更新する：
1. 機種ページの `og:image` → `https://kozelog.net/[maker-slug]/[machine-slug]/img/cabinet.png`
2. `admin/index.html` の該当機種に `hasImg: true` を追加
3. 機種ページの `machine-hero` セクション内 `machine-img-wrap` に筐体画像を表示する：
   ```html
   <img src="img/cabinet.png" alt="{機種名} 筐体画像" width="180" height="320" loading="lazy">
   ```
   - プレースホルダー（🎰絵文字・logo.png・SVG等）は筐体画像に置き換える
   - 筐体画像がない場合のみ `<div class="no-img-ph">🎰</div>` を使用する

### 画像解析フロー（筐体図・リール図柄）
```
1. ユーザーが画像を貼り付け
2. 筐体図 → 機種名・メーカーを特定
3. リール図柄 → リール配列をテキスト化
4. テキスト化した内容をユーザーに確認
5. OK → 該当機種ページのリール配列セクションに反映
6. 筐体画像をファイル保存した場合 → admin/index.html の該当機種の hasImg: true に更新
```

> ※ リール図柄の読み取り後は必ずユーザー確認を行う

---

## 8. ファイル・フォルダ管理

### ディレクトリ構成
```
Claude/
├── CLAUDE.md                        ← ルールブック
├── index.html                       ← TOPページ
├── assets/
│   ├── common.css                   ← 共通スタイル
│   ├── common.js                    ← 共通JS（loadComponents等）
│   ├── site.js                      ← GTM等
│   ├── header.html                  ← 共通ヘッダー
│   ├── footer.html                  ← 共通フッター
│   ├── top-hero.js                  ← TOPページヒーローアニメーション
│   ├── machines/
│   │   ├── index.js                 ← 全機種マスターリスト
│   │   └── [machine-id].js          ← 機種別データ
│   └── templates/
│       ├── normal.html              ← ノーマル機テンプレート
│       ├── at-art.html              ← AT/ART機テンプレート
│       └── sumasuro-normal.html     ← スマスロ（ノーマル）テンプレート
├── machine/                         ← 機種データ（第3階層）
│   └── [maker-slug]/                ← メーカー別フォルダ
│       ├── index.html               ← メーカー一覧ページ
│       └── [machine-slug]/
│           └── index.html           ← 機種ページ
```

### 命名規則
- フォルダ名・ファイル名：**英数字・ハイフン小文字**（例: `smart-hanabi`, `isekai-quartet`）
- 機種データJS：`assets/machines/[machine-id].js`

### 共通アセットの使い方
- `loadComponents(ASSETS_BASE)` で header/footer を注入
- ルートページ：`ASSETS_BASE = 'assets/'`
- メーカー一覧ページ（2階層）：`ASSETS_BASE = '../../assets/'`
- 機種ページ（3階層）：`ASSETS_BASE = '../../../assets/'`

---

## 9. 技術仕様

### 基本方針
- 純粋な静的HTML/CSS/JS（ビルドシステムなし）
- デプロイ：GitHub Pages + Cloudflare

### デザイン規則
| 変数 | 値 | 用途 |
|---|---|---|
| `--gold` | `#00c8e0` | アクセントカラー（シアン） |
| `--bg` | `#141418` | 背景色 |
| `--text` | `#e0e8f0` | テキスト色 |

### 設定色
| 設定 | カラーコード |
|---|---|
| 設定1 | `#888` |
| 設定2 | `#5ba0b5` |
| 設定3 | `#4fa880` |
| 設定4 | `#7abb50` |
| 設定5 | `#f5b030` |
| 設定6 | `#f05050` |

---

## 10. SEO施策ルール

### ① 構造化データ（JSON-LD）

**機種ページのFAQ生成手順：**
新機種追加時または既存ページ更新時、以下の手順でFAQを自律生成する。

```
1. 機種名を確認する
2. WebSearchで下記クエリを検索する
   - 「{機種名} よくある質問」
   - 「{機種名} 打ち方」
   - 「{機種名} 設定判別」
   - 「{機種名} やめ時」
   - 「{機種名} 天井」（AT/ART機のみ）
3. 検索結果からユーザーニーズの高い質問を抽出
4. ニーズ順に5〜8項目のFAQを構成し、既存解析データを参照して回答文を生成
5. 回答文は必ずページ内の実数値と整合させる
```

**FAQの構造化マークアップ標準化ルール：**
HTMLの `<details class="faq-item">` とJSON-LDの `FAQPage mainEntity` は**常に完全同期**させる。

| チェック項目 | ルール |
|---|---|
| 質問文 | `<summary>` のテキストとJSON-LDの `"name"` を**一字一句一致**させる |
| 回答文 | `<div class="faq-a">` のテキストとJSON-LDの `"text"` を**一致**させる |
| 項目数 | HTMLのFAQ件数とJSON-LDの `mainEntity` 配列件数を**必ず一致**させる |
| 更新タイミング | FAQ追加・削除・修正時はHTML・JSON-LDを**必ず同時に更新**する |

> Googleのリッチリザルト審査では「JSON-LDの内容がページ上に視覚的に存在すること」が必須。片方だけの更新は審査落ちの原因となるため厳守する。

**機種ページに実装するスキーマ：**
- `FAQPage`：上記手順で生成したFAQ。urlは `https://kozelog.com/{path}/#faq`
- `BreadcrumbList`：トップ > メーカー > 機種ページ の3階層
- `WebPage`：ページ名・説明・正規URL
- `Article`：headline・datePublished・author

**トップ・一覧ページに実装するスキーマ：**
- `BreadcrumbList`のみ

**パンくずの表記揺れ網羅ルール：**
メーカー名など日本語・英語の両表記が存在する場合、`BreadcrumbList` JSON-LD の `name` およびHTMLパンくずの表示テキストは**両形式を必ず含める**こと。

| メーカー | 正しい表記 |
|---|---|
| Sammy | `Sammy（サミー）` |
| 北電子 | `北電子（KITADENSHI）` |
| アクロス | `アクロス（ACROSS）` |
| エレコ | `エレコ（ELECO）` |
| オリンピア・平和 | `オリンピア（OLYMPIA）` |
| 山佐 | `山佐（YAMASA）` |
| パイオニア | `パイオニア（PIONEER）` |
| SANKYO | `SANKYO（三共）` |
| オーイズミ | `オーイズミ（OIZUMI）` |
| JFJ | `JFJ` |
| 岡崎産業 | `岡崎産業` |
| 山佐ネクスト | `山佐ネクスト` |
| D-light | `D-light（ディ・ライト）` |
| 大都技研 | `大都技研` |
| サンスリー | `サンスリー` |
| ミズホ | `ミズホ` |
| ビスティ | `ビスティ` |

> ※ 新メーカー追加時はこの表に随時追記する

### ② セクション構造（§6との統一ルール）

| セクション | id | h2パターン |
|---|---|---|
| スペック・機械割 | `spec` | `{機種名} スペック・機械割` |
| 小役確率 | `koyaku` | `{機種名} 小役確率` |
| 設定判別 | `setting` | `{機種名} 設定判別ポイント` |
| 天井・リセット | `ceiling` | `{機種名} 天井・リセット` |
| ST詳細 | `sec-st` | `{機種名}（ST名）詳細` |
| フリーズ・高確率ゾーン | `sec-freeze` | `{機種名} フリーズ・{ゾーン名}` |
| 有利区間 | `sec-yuuri` | `{機種名} 有利区間` |
| 打ち方 | `how-to-play` | `{機種名} 打ち方・技術介入` |
| ボーナス概要 | `bonus` | `{機種名} ボーナス概要` |
| 設定変更・朝一恩恵 | `sec-morning` | `{機種名} 設定変更・朝一恩恵` |
| 設定判別ツール | `tool` | `{機種名} 設定判別ツール` |
| FAQ | `faq` | `{機種名} よくある質問` |
| 関連機種 | `related` | `関連機種` |

- 各セクションに `<section id="...">` タグを使用
- `h2` には必ず機種名を含める（`tool` と `related` は除く）

### ③ OGP設定

全ページ共通（`<head>` 内に記述）：
```html
<meta property="og:title" content="ページタイトル">
<meta property="og:description" content="ページの説明">
<meta property="og:type" content="website">  <!-- 機種ページは article -->
<meta property="og:url" content="正規URL">
<meta name="twitter:card" content="summary">
<meta property="og:image" content="https://kozelog.net/assets/logo.png">
```

**og:image 切り替えルール：**
| 条件 | og:image の値 |
|---|---|
| 筐体画像なし（デフォルト） | `https://kozelog.net/assets/logo.png` |
| 筐体画像あり（`hasImg: true`） | `https://kozelog.net/{maker-slug}/{machine-slug}/img/cabinet.png` |

- 筐体画像を保存したら `admin/index.html` の `hasImg: true` を更新し、同時に該当ページの `og:image` を筐体画像URLに差し替える
- OGP用画像の推奨サイズ：1200×630px以上

### ④ 内部リンク

- 機種ページ下部に `<section id="related">` を設置し、3〜5件の関連機種リンクを掲載
- 関連機種の選定基準：同メーカー優先、次に同タイプ（ノーマル機同士等）

### ⑤ sitemap.xml

- `sitemap.xml` はルート（`/`）に設置
- 手動または `generate-sitemap.js` スクリプトで生成
- 各URLに `<lastmod>`・`<changefreq>`・`<priority>` を含める
- 優先度目安：トップ `1.0` / メーカー一覧 `0.8` / 機種ページ `0.7`
- `robots.txt` に `Sitemap: https://kozelog.com/sitemap.xml` を記述
- 機種追加時は必ず `sitemap.xml` を更新すること

### ⑦ CTAボタン × 広告（未実装・仕込みメモ）

- 設定判別ツールの「設定を判別する」ボタン（`.calc-btn`）クリック時に広告を発動する予定
- **実装方式：GTM推奨**（`site.js` でGTM導入済みのため、コード変更不要）
  - GTMトリガー：クリック → CSSセレクター `.calc-btn`
  - 広告タグはGTM側で管理
- コード実装の場合は `common.js` の `calcSetting()` 内にフック1箇所を追加（全ページ波及させない）
- 各ページの個別実装は禁止（12ページ以上の修正が発生するため）

### ⑧ Core Web Vitals

- `<img>` には `width`・`height` 属性を必ず付与（CLS防止）
- ファーストビュー以外の画像には `loading="lazy"` を付与
- ファーストビューと無関係な外部スクリプトには `defer` または `async` を付与

---

## 11. 個別追加コンテンツ記録

既存ページに**後から個別追加したコンテンツ**は `admin/index.html` の MACHINES 配列の各エントリに記録する。

### 記録場所
`admin/index.html` の MACHINES 配列 → 該当機種の `addedContent` フィールド（配列）

```javascript
// 例：カバネリ2に設定示唆演出・穢れ等を追加した場合
{
  name: 'スマスロ 甲鉄城のカバネリ 海門決戦',
  addedContent: ['設定示唆演出一覧', '穢れシステム', 'やめ時まとめ', 'FAQ拡充(7→15件)']
}
```

### ルール
- 初期作成時のコンテンツは `addedContent` に記載しない
- 既存ページにセクション・FAQなどを後から追加した場合は、**作業完了後に必ず** `addedContent` を更新する
- admin/index.html の「追加コンテンツ」列にチップ形式で表示される
